type ToggleFollowingResponse {
  ok: Boolean!
  err: String
}

enum ActionOptions {
  FOLLOWING
  UNFOLLOWING
}

type Mutation {
  ToggleFollowing(id: Int!, action: String!): ToggleFollowingResponse!
}
